# Demo

The Demo Shell remains an event-driven observer around real product routes. It cannot advance steps without real typed events and it cannot create model output, tool output, diagnosis evidence or governance state.

The existing video and storyboard are marked `ARCHIVED_PRE_LLM_WALKTHROUGH`. This change does not add a new recording or narrative layer.
