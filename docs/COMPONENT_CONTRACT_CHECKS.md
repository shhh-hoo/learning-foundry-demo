# Component Contract Checks

`runComponentContractChecks` returns `ComponentContractCheckReport`. Checks cover schema, curriculum alignment, reasoning graph integrity, deterministic recomputation, target consistency, units, precision, mark alignment, hint references and runtime compatibility.

These checks govern component publication. They are separate from Learner Diagnosis, Runtime Validation, Automated Tests and AgentEval.
